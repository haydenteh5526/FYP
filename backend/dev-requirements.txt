# Development & CI tooling (not needed at runtime).
# Install with: pip install -r requirements.txt -r dev-requirements.txt
# Versions pinned to match the proven CI toolchain.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.16.0
